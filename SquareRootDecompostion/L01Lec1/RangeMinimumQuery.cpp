#include <iostream>
#include <bits/stdc++.h>
using namespace std;
#define MAXN 100000
int arr[MAXN];
int sTable[317];

void initialize(int n) {
    int sqTableSize = ceil(sqrt(n));

    
}
int main() {

    int size;
    cin >> size;

    for(int i = 0; i < size; ++i) {
        cin >> arr[i];
    }



}